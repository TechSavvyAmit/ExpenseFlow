#include "algorithms/DebtSimplifier.h"

std::map<std::string,
    std::map<std::string, double>>
DebtSimplifier::simplify(
    std::map<std::string,
    std::map<std::string, double>>& balances
) {
    return balances;
}